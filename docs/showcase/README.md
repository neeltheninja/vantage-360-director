# Add showcase panoramas here

Add any AVIF, GIF, JPEG, PNG, or WebP image to this folder. Vantage discovers
it automatically on the next development-server restart or production build.

Use a descriptive filename because the filename becomes the gallery title. No
manifest or code edit is needed.
